appId: com.electron.app
productName: kira-encryptor
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**
  - node_modules/**/*.node
  - node_modules/**/*.exe
fileAssociations:
  - ext: kira
    role: Editor
    name: KIRA Encryptor File
    icon: build/kira-file.ico
    description: File encrypted by KIRA Encryptor
win:
  target:
    - portable
    - nsis
  executableName: kira-encryptor
nsis:
  artifactName: ${productName}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
mac:
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
    - NSPublicFolderUsageDescription: Application requests access to the user's Public folder.
    - NSDesktopFolderUsageDescription: Application requests access to the user's Desktop folder.
    - CFBundleDocumentTypes:
      - CFBundleTypeName: KIRA Encryptor File
        CFBundleTypeExtensions:
          - kira
        CFBundleTypeRole: Editor
        CFBundleTypeIconFile: kira-file.icns
  notarize: false
dmg:
  artifactName: ${productName}-${version}.${ext}
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: ${productName}-${version}.${ext}
npmRebuild: false
publish:
  provider: generic
  url: https://example.com/auto-updates
